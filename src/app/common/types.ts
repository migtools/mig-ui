export interface IMigMetaVars {
  clusterApi: string;
  devServerPort: string;
  userScope: string;
  clientId: string;
  redirectUri: string;
  namespace: string;
  configNamespace: string;
  oauthClientSecret: string;
  discoveryApi: string;
  hookRunnerImage: string;
}
